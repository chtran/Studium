%hr
%div.row-fluid.wallpost-container
  %div.span2
    %div.thumbnail.thumbnail-wall-image
      = image_tag(User.find(w.sender_id).profile.image, :class => 'wall-image')
  %div.span10.wallpost-content-container
    %div.arrow-container
      %div.arrow
    %div.wallpost
      %div.wallpost-content.row-fluid
        %div.span9
          %h4= User.find(w.sender_id).name
        %div.span3
          %p.time-ago= w.created_at.to_date
        %p= w.content
