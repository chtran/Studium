%div.row-fluid{class: @style}
  %p= @message

- @choices.each do |choice|
  = render partial: "choices/choice",locals: {choice: choice[:data],choice_class: "disabled #{choice[:result]}"}

:javascript
  $(".correct").addClass("btn-success")
               .append("<div class='span2'><i class='icon-ok icon-white' /></div>")
  $(".selected").addClass("btn-danger")
                .append("<div class='span2'>Your choice</div>")
